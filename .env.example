# Optional — shared secret for larv.ai's POST /api/cv/spend. Without it,
# /api/cv-spend relays through leftclaw.services instead (works, but set this
# in production).
CV_SPEND_SECRET=

# Optional — server-side Base RPC (signature verification, job reads).
BASE_RPC_URL=https://mainnet.base.org
